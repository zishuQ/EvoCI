# Continual benchmark manifests

Create ordered family manifests here. Each family should contain at least three tasks from different
repositories where possible: the first task may teach a capability, and later tasks measure reuse.
Ground truth remains evaluator-only through the typed benchmark adapter.

